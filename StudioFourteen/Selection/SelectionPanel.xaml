<windows:Panel
	x:Class="StudioFourteen.Selection.SelectionPanel"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:s14="clr-namespace:StudioFourteen.Xaml"
	xmlns:converters="clr-namespace:StudioFourteen.Xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:gizmos="clr-namespace:StudioFourteen.Rendering.Draw.Gizmos"
	xmlns:icons="clr-namespace:StudioFourteen.Icons"
	xmlns:librarySelector="clr-namespace:StudioFourteen.Library.Selector"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:mvm="clr-namespace:StudioFourteen.Mvm"
	xmlns:sys="clr-namespace:System;assembly=mscorlib"
	xmlns:windows="clr-namespace:StudioFourteen.Panels"
	Width="600"
	Height="58"
	HorizontalAlignment="Center"
	d:DataContext="{Binding Path=., RelativeSource={RelativeSource Mode=Self}}"
	DefaultPosition="0.5,0"
	HostStyle="{DynamicResource PanelToolbarStyle}"
	RememberWindowState="False"
	mc:Ignorable="d">

	<Grid x:Name="MainBorder" Height="28">

		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>

		<ComboBox
			Grid.Column="0"
			ItemsSource="{Binding Types}"
			SelectedValue="{Binding CurrentType}">
			<ComboBox.ItemTemplate>
				<DataTemplate>
					<icons:IconView Icon="{Binding Icon}" />
				</DataTemplate>
			</ComboBox.ItemTemplate>
		</ComboBox>

		<s14:SwitchSelector
			Grid.Column="1"
			HorizontalContentAlignment="Center"
			ItemsSource="{Binding CurrentType.Objects}"
			SelectedItem="{Binding CurrentType.Selection}"
			Visibility="{Binding CurrentType.Objects.Count, Converter={StaticResource LessThanToVisibility}, ConverterParameter=9}">

			<ListBox.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding Name}" />
				</DataTemplate>
			</ListBox.ItemTemplate>
		</s14:SwitchSelector>

		<ComboBox Grid.Column="1"
			HorizontalContentAlignment="Center"
			ItemsSource="{Binding CurrentType.Objects}"
			SelectedItem="{Binding CurrentType.Selection}"
			Visibility="{Binding CurrentType.Objects.Count, Converter={StaticResource GreaterThanToVisibility}, ConverterParameter=8}">
			<ComboBox.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding Name}" />
				</DataTemplate>
			</ComboBox.ItemTemplate>
		</ComboBox>

		<ToggleButton
			x:Name="AddButton"
			Grid.Column="2"
			Width="32"
			Content="{DynamicResource ICON_AddObject}"
			Style="{DynamicResource IconToggleButton}" />

		<librarySelector:LibrarySelector
			HorizontalAlignment="Center"
			CloseOnSelection="True"
			IsOpen="{Binding ElementName=AddButton, Path=IsChecked}"
			PlacementTarget="{Binding ElementName=AddButton}"
			SelectedItem="{Binding SelectedAdd}"
			Types="{Binding CurrentCreateType.Types}"
			StaysOpen="False">

			<librarySelector:LibrarySelector.Header>
				<s14:SwitchSelector
					Grid.Column="0"
					Height="32"
					ItemsSource="{Binding CreateTypes}"
					SelectedValue="{Binding CurrentCreateType}">
					<s14:SwitchSelector.ItemTemplate>
						<DataTemplate>
							<icons:IconView
								Width="24"
								Height="24"
								Icon="{Binding Icon}"
								ToolTip="{Binding Name}" />
						</DataTemplate>
					</s14:SwitchSelector.ItemTemplate>
				</s14:SwitchSelector>
			</librarySelector:LibrarySelector.Header>
		</librarySelector:LibrarySelector>

	</Grid>
</windows:Panel>
