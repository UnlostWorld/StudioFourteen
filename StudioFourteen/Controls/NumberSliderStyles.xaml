<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="clr-namespace:StudioFourteen.Controls"
	xmlns:wpfUtils="clr-namespace:WpfUtils.Controls;assembly=WpfUtils">

	<ControlTemplate x:Key="NumberSliderTemplate" TargetType="{x:Type local:NumberSlider}">
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition />
			</Grid.ColumnDefinitions>

			<local:Slider
				x:Name="TestSlider"
				Maximum="{TemplateBinding Maximum}"
				Minimum="{TemplateBinding Minimum}"
				Value="{Binding Value, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" />

			<wpfUtils:NumberBox
				Buttons="True"
				Grid.Column="1"
				Maximum="{TemplateBinding Maximum}"
				Minimum="{TemplateBinding Minimum}"
				Value="{Binding Value, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" />

		</Grid>
	</ControlTemplate>

	<Style TargetType="{x:Type local:NumberSlider}" >
		<Setter Property="Template" Value="{StaticResource NumberSliderTemplate}"/>
		<Setter Property="HorizontalAlignment" Value="Stretch"/>
	</Style>

</ResourceDictionary>