<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="clr-namespace:StudioFourteen.Controls"
	xmlns:wpfUtils="clr-namespace:WpfUtils.Controls;assembly=WpfUtils">


	<ControlTemplate x:Key="MultiNumberBoxTemplate" TargetType="{x:Type local:MultiNumberBox}">
		<Grid>
			<ItemsControl ItemsSource="{TemplateBinding Channels}">
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="22" />
								<ColumnDefinition />
							</Grid.ColumnDefinitions>

							<TextBlock
								Grid.Column="0"
								Margin="3,0"
								Style="{DynamicResource RightLabel}"
								Text="{Binding Label}" />

							<local:NumberBox
								Grid.Row="0"
								Grid.Column="1"
								Change="{Binding Change}"
								Maximum="{Binding Maximum}"
								Minimum="{Binding Minimum}"
								Wrap="{Binding Wrap}"
								Value="{Binding Value}" />
						</Grid>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</Grid>
	</ControlTemplate>

	<ControlTemplate x:Key="MultiNumberBoxSingleLineTemplate" TargetType="{x:Type local:MultiNumberBox}">
		<Grid>
			<ItemsControl ItemsSource="{TemplateBinding Channels}">

				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<UniformGrid Rows="1"/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>

				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<local:NumberBox
							Grid.Row="0"
							Grid.Column="1"
							Change="{Binding Change}"
							Maximum="{Binding Maximum}"
							Minimum="{Binding Minimum}"
							Wrap="{Binding Wrap}"
							Value="{Binding Value}" />
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</Grid>
	</ControlTemplate>

	<Style x:Key="StudioMultiNumberBox" TargetType="{x:Type local:MultiNumberBox}">
		<Setter Property="Template" Value="{StaticResource MultiNumberBoxTemplate}" />

		<Style.Triggers>
			<Trigger Property="SingleLine" Value="True">
				<Setter Property="Template" Value="{StaticResource MultiNumberBoxSingleLineTemplate}" />
			</Trigger>
		</Style.Triggers>
	</Style>

	<Style BasedOn="{StaticResource StudioMultiNumberBox}" TargetType="{x:Type local:Color4Box}">
		<Setter Property="Change" Value="0.1" />
		<Setter Property="Maximum" Value="1" />
		<Setter Property="Minimum" Value="0" />
	</Style>

	<Style BasedOn="{StaticResource StudioMultiNumberBox}" TargetType="{x:Type local:Color3Box}">
		<Setter Property="Change" Value="0.1" />
		<Setter Property="Maximum" Value="1" />
		<Setter Property="Minimum" Value="0" />
	</Style>


	<Style BasedOn="{StaticResource StudioMultiNumberBox}" TargetType="{x:Type local:Vector4Box}" />

	<Style BasedOn="{StaticResource StudioMultiNumberBox}" TargetType="{x:Type local:Vector3Box}" />

	<Style BasedOn="{StaticResource StudioMultiNumberBox}" TargetType="{x:Type local:Vector2Box}" />

	<Style BasedOn="{StaticResource StudioMultiNumberBox}" TargetType="{x:Type local:QuaternionBox}">
		<Setter Property="Change" Value="5" />
		<Setter Property="Maximum" Value="180" />
		<Setter Property="Minimum" Value="-180" />
		<Setter Property="Wrap" Value="True" />
	</Style>

</ResourceDictionary>
