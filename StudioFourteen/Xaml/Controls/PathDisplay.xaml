<ItemsControl
	x:Class="StudioFourteen.Xaml.PathDisplay"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:s14="clr-namespace:StudioFourteen.Xaml"
	xmlns:converters="clr-namespace:StudioFourteen.Xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:icons="clr-namespace:StudioFourteen.Icons"
	xmlns:local="clr-namespace:StudioFourteen.Xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Margin="1"
	d:DesignWidth="400"
	mc:Ignorable="d">

	<ItemsControl.ItemsPanel>
		<ItemsPanelTemplate>
			<WrapPanel />
		</ItemsPanelTemplate>
	</ItemsControl.ItemsPanel>

	<ItemsControl.ItemTemplate>
		<DataTemplate DataType="{x:Type local:PathSegment}">
			<StackPanel Orientation="Horizontal">
				<icons:IconView
					x:Name="Icon"
					Width="14"
					Margin="0,4,4,2"
					Icon="{Binding Icon}" />

				<icons:IconView
					x:Name="Caret"
					Margin="6,4,6,2"
					Icon="{DynamicResource ICON_PathSeperator}"
					Visibility="Collapsed" />

				<TextBlock Margin="0,2,0,0" Text="{Binding Name}" />
			</StackPanel>

			<DataTemplate.Triggers>
				<DataTrigger Binding="{Binding Icon}" Value="{x:Null}">
					<Setter TargetName="Caret" Property="Visibility" Value="Visible" />
					<Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
				</DataTrigger>
			</DataTemplate.Triggers>
		</DataTemplate>
	</ItemsControl.ItemTemplate>
</ItemsControl>