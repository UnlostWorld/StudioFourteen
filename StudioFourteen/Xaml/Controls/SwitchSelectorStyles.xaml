<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:s14="clr-namespace:StudioFourteen.Xaml">

	<Style x:Key="SwitchSelectorItemStyle" TargetType="{x:Type ListBoxItem}">
		<Setter Property="Focusable" Value="False" />
		<Setter Property="HorizontalContentAlignment" Value="Center" />

		<Setter Property="Margin" Value="0" />
		<Setter Property="Padding" Value="6, 4" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type ListBoxItem}">

					<RadioButton
						x:Name="Button"
						Height="Auto"
						Margin="0"
						Padding="{TemplateBinding Padding}"
						HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
						s14:RadioButtonHighlighter.UseGroupHighlight="True"
						Background="Orange"
						Focusable="False"
						IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}"
						IsEnabled="{TemplateBinding IsEnabled}"
						Style="{DynamicResource RadioButtonNoHighlight}">

						<ContentPresenter
							x:Name="Content"
							HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />

					</RadioButton>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
		<Setter Property="TextBlock.Foreground" Value="{DynamicResource ForegroundLightBrush}" />

		<Style.Triggers>
			<Trigger Property="IsSelected" Value="True">
				<Setter Property="TextBlock.Foreground" Value="{DynamicResource TrimBrush}" />
			</Trigger>
		</Style.Triggers>
	</Style>

	<Style x:Key="SwitchSelectorNoBorder" TargetType="{x:Type s14:SwitchSelector}">
		<Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
		<Setter Property="FocusVisualStyle" Value="{DynamicResource FocusVisualStyle}" />
		<Setter Property="Focusable" Value="True" />

		<Setter Property="Height" Value="26" />
		<Setter Property="IsTabStop" Value="True" />

		<Setter Property="ItemContainerStyle"
			Value="{DynamicResource SwitchSelectorItemStyle}" />

		<Setter Property="ItemsPanel">
			<Setter.Value>
				<ItemsPanelTemplate>
					<UniformGrid Rows="1" />
				</ItemsPanelTemplate>
			</Setter.Value>
		</Setter>
		<Setter Property="KeyboardNavigation.DirectionalNavigation" Value="Continue" />
		<Setter Property="Margin" Value="1" />
		<Setter Property="Padding" Value="0" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type s14:SwitchSelector}">
					<s14:RadioButtonHighlighter>
						<ItemsPresenter Margin="{TemplateBinding Padding}"
							SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
					</s14:RadioButtonHighlighter>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style
		x:Key="SwitchSelector"
		BasedOn="{StaticResource SwitchSelectorNoBorder}"
		TargetType="{x:Type s14:SwitchSelector}">

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type s14:SwitchSelector}">
					<Border Background="{TemplateBinding Background}"
						Style="{DynamicResource ControlBorder}">
						<s14:RadioButtonHighlighter>
							<ItemsPresenter
								SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
						</s14:RadioButtonHighlighter>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style
		x:Key="SwitchSelectorVertical"
		BasedOn="{StaticResource SwitchSelector}"
		TargetType="{x:Type s14:SwitchSelector}">

		<Setter Property="Height" Value="Auto" />

		<Setter Property="ItemContainerStyle">
			<Setter.Value>
				<Style BasedOn="{StaticResource SwitchSelectorItemStyle}"
					TargetType="{x:Type ListBoxItem}">
					<Setter Property="HorizontalContentAlignment" Value="stretch" />
					<Setter Property="Padding" Value="0,6" />
				</Style>
			</Setter.Value>
		</Setter>


		<Setter Property="ItemsPanel">
			<Setter.Value>
				<ItemsPanelTemplate>
					<StackPanel />
				</ItemsPanelTemplate>
			</Setter.Value>
		</Setter>

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type s14:SwitchSelector}">
					<Border Background="{TemplateBinding Background}"
						Style="{DynamicResource ControlBorder}">
						<s14:RadioButtonHighlighter Orientation="Vertical">
							<ItemsPresenter
								SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
						</s14:RadioButtonHighlighter>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style BasedOn="{StaticResource SwitchSelector}"
		TargetType="{x:Type s14:SwitchSelector}" />

</ResourceDictionary>