<UserControl
	x:Class="StudioFourteen.Tags.TagDisplay"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:converters="clr-namespace:WpfUtils.Converters;assembly=WpfUtils"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:StudioFourteen.Tags"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	d:DataContext="{Binding Path=., RelativeSource={RelativeSource Mode=Self}}"
	d:DesignHeight="300"
	d:DesignWidth="300"
	mc:Ignorable="d">

	<UserControl.Resources>
		<converters:NotZeroToVisibilityConverter x:Key="NotZeroToVisibility" />

		<DataTemplate x:Key="TagItem">

			<Button
				Margin="0"
				Click="OnTagClicked"
				Content="{Binding DisplayName}"
				ToolTip="{Binding ToolTip}">
				<Button.Template>
					<ControlTemplate TargetType="{x:Type Button}">
						<Grid
							Height="20"
							Margin="2,2">

							<Border
								Background="{DynamicResource ForegroundLightBrush}"
								CornerRadius="10,4,4,10"
								Opacity="0.5" />

							<Border
								x:Name="HoverBorder"
								Background="{DynamicResource ControlHighlightBrush}"
								CornerRadius="10,4,4,10"
								Opacity="0" />

							<Ellipse
								Width="10"
								Height="10"
								Margin="4"
								HorizontalAlignment="Left"
								Fill="{DynamicResource BackgroundBrush}" />

							<ContentPresenter
								Margin="18,2,6,2"
								Content="{TemplateBinding Content}"
								ContentTemplate="{TemplateBinding ContentTemplate}"
								TextBlock.Foreground="{DynamicResource ControlBackgroundBrush}" />
						</Grid>

						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Trigger.EnterActions>
									<BeginStoryboard>
										<Storyboard>
											<DoubleAnimation
												Storyboard.TargetName="HoverBorder"
												Storyboard.TargetProperty="Opacity"
												To="1"
												Duration="0:00:00.150">
												<DoubleAnimation.EasingFunction>
													<SineEase EasingMode="EaseOut" />
												</DoubleAnimation.EasingFunction>
											</DoubleAnimation>

										</Storyboard>
									</BeginStoryboard>
								</Trigger.EnterActions>
								<Trigger.ExitActions>
									<BeginStoryboard>
										<Storyboard>
											<DoubleAnimation
												Storyboard.TargetName="HoverBorder"
												Storyboard.TargetProperty="Opacity"
												To="0"
												Duration="0:00:00.250">
												<DoubleAnimation.EasingFunction>
													<SineEase EasingMode="EaseIn" />
												</DoubleAnimation.EasingFunction>
											</DoubleAnimation>
										</Storyboard>
									</BeginStoryboard>
								</Trigger.ExitActions>
							</Trigger>
						</ControlTemplate.Triggers>

					</ControlTemplate>
				</Button.Template>
			</Button>



		</DataTemplate>

	</UserControl.Resources>

	<Grid
		x:Name="ContentArea"
		Visibility="{Binding Tags.Count, Converter={StaticResource NotZeroToVisibility}}">
		<ItemsControl
			Margin="3"
			ItemTemplate="{StaticResource TagItem}"
			ItemsSource="{Binding Tags}"
			ScrollViewer.HorizontalScrollBarVisibility="Disabled">

			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<WrapPanel IsItemsHost="True" />
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
		</ItemsControl>
	</Grid>
</UserControl>
