<ResourceDictionary>

	<Color x:Key="InputBackground">#151515</Color>
	<Color x:Key="Foreground">#ECECEC</Color>
	<Color x:Key="Trim">#FF1493</Color>

	<!-- Button -->
	<ControlTheme x:Key="{x:Type Button}" TargetType="{x:Type Button}">
		<Setter Property="Margin" Value="4" />
		<Setter Property="Height" Value="32" />
		<Setter Property="Background" Value="#888888" />
		<Setter Property="Foreground" Value="{DynamicResource Foreground}" />
		<Setter Property="Padding" Value="16, 3" />
		<Setter Property="ClipToBounds" Value="False" />
		<Setter Property="Cursor" Value="Hand" />
		<Setter Property="Template">
			<ControlTemplate>
				<Panel>
					<Border
						CornerRadius="16"
						HorizontalAlignment="Stretch"
						VerticalAlignment="Stretch"
						BorderThickness="1"
						BorderBrush="#99000000">
						<Border
							CornerRadius="15"
							HorizontalAlignment="Stretch"
							VerticalAlignment="Stretch"
							BorderThickness="1"
							BorderBrush="#99666666">
							<Border
								Name="Border"
								CornerRadius="14"
								Opacity="0.25"
								HorizontalAlignment="Stretch"
								VerticalAlignment="Stretch"
								Background="{TemplateBinding Background}">
								<Border.Transitions>
									<Transitions>
										<DoubleTransition Property="Opacity" Duration="0:0:0.1" />
									</Transitions>
								</Border.Transitions>
							</Border>
						</Border>
					</Border>

					<Grid>
						<ContentPresenter
							Name="PART_ContentPresenter"
							HorizontalAlignment="Center"
							VerticalAlignment="Center"
							Margin="{TemplateBinding Padding}"
							Content="{TemplateBinding Content}" />
					</Grid>
				</Panel>
			</ControlTemplate>
		</Setter>

		<Style Selector="^:pointerover">
			<Style Selector="^ /template/ Border#Border">
				<Setter Property="Opacity" Value="0.6" />
			</Style>
		</Style>

		<Style Selector="^:pressed">
			<Style Selector="^ /template/ Border#Border">
				<Setter Property="Opacity" Value="0.4" />
			</Style>
		</Style>

	</ControlTheme>

	<!-- TextBox -->
	<ControlTheme x:Key="{x:Type TextBox}" TargetType="{x:Type TextBox}">
		<Setter Property="Margin" Value="4" />
		<Setter Property="CaretBrush" Value="{DynamicResource Foreground}" />
		<Setter Property="SelectionBrush" Value="{DynamicResource Background}" />
		<Setter Property="TextAlignment" Value="Center" />
		<Setter Property="Padding" Value="3" />
		<Setter Property="MinHeight" Value="32" />
		<Setter Property="Background" Value="{DynamicResource InputBackground}" />
		<Setter Property="Foreground" Value="{DynamicResource Foreground}" />
		<Setter Property="Cursor" Value="Ibeam" />
		<Setter Property="Template">
			<ControlTemplate>
				<Panel>
					<Border
						Margin="0"
						CornerRadius="16"
						HorizontalAlignment="Stretch"
						VerticalAlignment="Stretch"
						BorderThickness="1"
						BorderBrush="#99000000">
						<Border
							Name="Border"
							CornerRadius="15"
							HorizontalAlignment="Stretch"
							VerticalAlignment="Stretch"
							Background="#99000000"
							BorderThickness="1"
							BorderBrush="#99666666" />
					</Border>

					<ScrollViewer Name="PART_ScrollViewer"
						Grid.Column="1"
						Grid.ColumnSpan="1"
						AllowAutoHide="{TemplateBinding (ScrollViewer.AllowAutoHide)}"
						BringIntoViewOnFocusChange="{TemplateBinding (ScrollViewer.BringIntoViewOnFocusChange)}"
						HorizontalScrollBarVisibility="{TemplateBinding (ScrollViewer.HorizontalScrollBarVisibility)}"
						IsScrollChainingEnabled="{TemplateBinding (ScrollViewer.IsScrollChainingEnabled)}"
						VerticalScrollBarVisibility="{TemplateBinding (ScrollViewer.VerticalScrollBarVisibility)}">

						<Panel>

							<TextBlock
								Opacity="0.5"
								Text="{TemplateBinding Watermark}"
								HorizontalAlignment="Left"
								VerticalAlignment="Center"
								Margin="16,0,0,0" />

							<TextPresenter
								Name="PART_TextPresenter"
								Margin="7"
								HorizontalAlignment="Stretch"
								CaretBlinkInterval="{TemplateBinding CaretBlinkInterval}"
								CaretBrush="{TemplateBinding CaretBrush}"
								CaretIndex="{TemplateBinding CaretIndex}"
								LineHeight="{TemplateBinding LineHeight}"
								LetterSpacing="{TemplateBinding LetterSpacing}"
								PasswordChar="{TemplateBinding PasswordChar}"
								RevealPassword="{TemplateBinding RevealPassword}"
								SelectionBrush="{TemplateBinding SelectionBrush}"
								SelectionEnd="{TemplateBinding SelectionEnd}"
								SelectionForegroundBrush="{TemplateBinding SelectionForegroundBrush}"
								SelectionStart="{TemplateBinding SelectionStart}"
								Text="{TemplateBinding Text, Mode=TwoWay}"
								TextAlignment="{TemplateBinding TextAlignment}"
								TextWrapping="{TemplateBinding TextWrapping}" />
						</Panel>
					</ScrollViewer>
				</Panel>
			</ControlTemplate>
		</Setter>

		<Style Selector="^:focus /template/ Border#Border">
			<Setter Property="BorderBrush" Value="{DynamicResource Trim}" />
		</Style>

	</ControlTheme>

	<!-- TextBox -->
	<ControlTheme x:Key="{x:Type TextBlock}" TargetType="{x:Type TextBlock}">
		<Setter Property="Foreground" Value="{DynamicResource Foreground}" />
	</ControlTheme>

	<!-- Svg -->
	<ControlTheme x:Key="{x:Type StudioFourteen.Interface.Controls:Svg}"
		TargetType="{x:Type StudioFourteen.Interface.Controls:Svg}">
		<Setter Property="Foreground" Value="{DynamicResource Foreground}" />
	</ControlTheme>

	<!-- Tab Control -->
	<ControlTheme x:Key="{x:Type TabControl}"
		TargetType="{x:Type TabControl}">
		<Setter Property="Padding" Value="8" />

		<Setter Property="ItemsPanel">
			<Setter.Value>
				<ItemsPanelTemplate>
					<UniformGrid Rows="1" />
				</ItemsPanelTemplate>
			</Setter.Value>
		</Setter>

		<Setter Property="Template">
			<ControlTemplate>
				<DockPanel>
					<Panel Name="PART_TabStrip" Margin="2">
						<Border
							Margin="0"
							CornerRadius="999"
							HorizontalAlignment="Stretch"
							VerticalAlignment="Stretch"
							BorderThickness="1"
							BorderBrush="#99000000">
							<Border
								Name="Border"
								CornerRadius="999"
								HorizontalAlignment="Stretch"
								VerticalAlignment="Stretch"
								Background="#55000000"
								BorderThickness="1"
								BorderBrush="#99666666" />
						</Border>

						<ItemsPresenter Name="PART_ItemsPresenter"
							ItemsPanel="{TemplateBinding ItemsPanel}"
							HorizontalAlignment="Stretch"
							Margin="2" />
					</Panel>

					<ContentPresenter Name="PART_SelectedContentHost"
						Margin="{TemplateBinding Padding}"
						HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
						VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
						Content="{TemplateBinding SelectedContent}"
						ContentTemplate="{TemplateBinding SelectedContentTemplate}" />
				</DockPanel>

			</ControlTemplate>
		</Setter>
		<Style Selector="^[TabStripPlacement=Top]">
			<Setter Property="Padding" Value="0 4 0 0" />
		</Style>
		<Style Selector="^[TabStripPlacement=Top] /template/ Panel#PART_TabStrip">
			<Setter Property="DockPanel.Dock" Value="Top" />
		</Style>
		<Style Selector="^[TabStripPlacement=Bottom] /template/ Panel#PART_TabStrip">
			<Setter Property="DockPanel.Dock" Value="Bottom" />
		</Style>
		<Style Selector="^[TabStripPlacement=Bottom]">
			<Setter Property="Padding" Value="0 0 0 4" />
		</Style>
		<Style Selector="^[TabStripPlacement=Left] /template/ Panel#PART_TabStrip">
			<Setter Property="DockPanel.Dock" Value="Left" />
		</Style>
		<Style
			Selector="^[TabStripPlacement=Left] /template/ ItemsPresenter#PART_ItemsPresenter > WrapPanel">
			<Setter Property="Orientation" Value="Vertical" />
		</Style>
		<Style Selector="^[TabStripPlacement=Left]">
			<Setter Property="Padding" Value="4 0 0 0" />
		</Style>
		<Style Selector="^[TabStripPlacement=Right] /template/ Panel#PART_TabStrip">
			<Setter Property="DockPanel.Dock" Value="Right" />
		</Style>
		<Style
			Selector="^[TabStripPlacement=Right] /template/ ItemsPresenter#PART_ItemsPresenter > WrapPanel">
			<Setter Property="Orientation" Value="Vertical" />
		</Style>
		<Style Selector="^[TabStripPlacement=Right]">
			<Setter Property="Padding" Value="0 0 4 0" />
		</Style>
	</ControlTheme>

	<!-- Tab Item -->
	<ControlTheme x:Key="{x:Type TabItem}"
		TargetType="{x:Type TabItem}">
		<Setter Property="FontSize" Value="{DynamicResource FontSizeLarge}" />
		<Setter Property="Foreground" Value="{DynamicResource ThemeForegroundLowBrush}" />
		<Setter Property="HorizontalContentAlignment" Value="Center" />
		<Setter Property="Padding" Value="3" />
		<Setter Property="Margin" Value="2" />
		<Setter Property="Template">
			<ControlTemplate>
				<Panel>
					<Border
						Name="Border"
						CornerRadius="999"
						HorizontalAlignment="Stretch"
						VerticalAlignment="Stretch"
						Background="#77666666"
						Opacity="0">
						<Border.Transitions>
							<Transitions>
								<DoubleTransition Property="Opacity" Duration="0:0:0.1" />
							</Transitions>
						</Border.Transitions>
					</Border>

					<ContentPresenter Name="PART_ContentPresenter"
						Padding="{TemplateBinding Padding}"
						HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
						VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
						Content="{TemplateBinding Header}"
						ContentTemplate="{TemplateBinding HeaderTemplate}" />
				</Panel>
			</ControlTemplate>
		</Setter>

		<Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
			<Setter Property="RecognizesAccessKey" Value="True" />
		</Style>

		<Style Selector="^:disabled /template/ ContentPresenter#PART_ContentPresenter">
			<Setter Property="Opacity" Value="{DynamicResource ThemeDisabledOpacity}" />
		</Style>
		<Style Selector="^:pointerover /template/ Border#Border">
			<Setter Property="Opacity" Value="0.5" />
		</Style>
		<Style Selector="^:selected /template/ Border#Border">
			<Setter Property="Opacity" Value="1" />
		</Style>
		<Style Selector="^:selected:focus /template/ Border#Border">
			<Setter Property="Opacity" Value="0.5" />
		</Style>
		<Style Selector="^:selected:pointerover /template/ Border#Border">
			<Setter Property="Opacity" Value="0.5" />
		</Style>
		<Style
			Selector="^:selected:focus:pointerover /template/ Border#Border">
			<Setter Property="Opacity" Value="1" />
		</Style>
		<Style Selector="^[TabStripPlacement=Right]">
			<Setter Property="HorizontalContentAlignment" Value="Right" />
		</Style>
	</ControlTheme>

</ResourceDictionary>