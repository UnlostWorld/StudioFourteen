<ResourceDictionary>
	<ControlTheme x:Key="{x:Type S14:Svg}"
		TargetType="{x:Type S14:Svg}">
		<Setter Property="Foreground" Value="{DynamicResource Foreground}" />
	</ControlTheme>
</ResourceDictionary>