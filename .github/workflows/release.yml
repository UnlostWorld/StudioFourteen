name: Build Studio Fourteen

permissions:
  contents: write

on:
  push:
    branches: [ Live ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2
      with:
        fetch-depth: 0

    - name: Install .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 10.0.x

    - name: Download Dalamud
      run: |
        Invoke-WebRequest -Uri https://goatcorp.github.io/dalamud-distrib/stg/latest.zip -OutFile latest.zip
        Expand-Archive -Force latest.zip "$env:AppData\XIVLauncher\addon\Hooks\dev"

    - name: Build StudioFourteen
      run: dotnet build StudioFourteen.slnx /p:Configuration=Release /p:Version=${{ github.run_number }}

    - name: Zip Release
      uses: TheDoctor0/zip-release@0.6.0
      with:
        filename: '${{ github.workspace }}/StudioFourteen.zip'
        directory: '${{ github.workspace }}/StudioFourteen/bin/'
        exclusions: '*.pdb* *.xml*'

    - name: Upload Release
      uses: ncipollo/release-action@v1
      with:
        allowUpdates: true
        artifacts: '${{ github.workspace }}/StudioFourteen.zip'
        name: PeerSync v${{ github.run_number }}
        tag: v${{ github.run_number }}
        draft: false
        token: ${{ secrets.GITHUB_TOKEN }}
        commit: ${{ vars.GITHUB_SHA }}

    - name: Update repo.json
      run: |
        git config --global user.name "Actions User"
        git config --global user.email "actions@github.com"
        git fetch origin main && git checkout main
        $ver = '${{ github.run_number }}'
        $path = './Assets/repo.tmpl'
        $new_path = './repo.json'
        $content = get-content -path $path
        $content = $content -replace '{version}',$ver
        set-content -Path $new_path -Value $content
        git add repo.json
        git commit -m "[CI] Updating versions for v${{ github.run_number }}" || true
        git push origin main || true
