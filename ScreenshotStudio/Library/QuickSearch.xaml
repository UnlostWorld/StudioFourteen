<windows:DockPanel
	x:Class="ScreenshotStudio.Library.QuickSearch"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:converters="clr-namespace:WpfUtils.Converters;assembly=wpfUtils"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:ScreenshotStudio.Library"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:tags="clr-namespace:ScreenshotStudio.Tags"
	xmlns:windows="clr-namespace:ScreenshotStudio.Windows"
	xmlns:wpfUtils="clr-namespace:WpfUtils.Controls;assembly=WpfUtils"
	Width="450"
	Height="400"
	CanActivate="True"
	DockPosition="0.5, 0.5"
	ShowBackground="False"
	SizeToContent="Manual"
	mc:Ignorable="d">

	<Grid
		Height="400"
		HorizontalAlignment="Center">

		<Grid.Resources>
			<ResourceDictionary>
				<ResourceDictionary.MergedDictionaries>
					<ResourceDictionary Source="..\Studio\StudioButtonStyle.xaml" />
				</ResourceDictionary.MergedDictionaries>

				<BooleanToVisibilityConverter x:Key="B2V" />
				<converters:InvertedBoolToVisibilityConverter x:Key="!B2V" />


				<ControlTemplate
					x:Key="ListBoxItemTemplate"
					TargetType="{x:Type ListBoxItem}">

					<Border
						Margin="{TemplateBinding Margin}"
						Background="Transparent"
						BorderBrush="{TemplateBinding BorderBrush}"
						BorderThickness="{TemplateBinding BorderThickness}"
						ClipToBounds="{TemplateBinding ClipToBounds}"
						ToolTip="{TemplateBinding Content}">
						<Grid Background="Transparent">

							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition />
							</Grid.ColumnDefinitions>

							<Border
								x:Name="SelectionBorder"
								Grid.ColumnSpan="2"
								Opacity="0"
								Style="{DynamicResource WpfUtilsSelectedBorder}" />

							<Border
								x:Name="HoverBorder"
								Grid.Column="1"
								Margin="1"
								Background="{DynamicResource BackgroundLightBrush}"
								BorderBrush="{DynamicResource BackgroundLightBrush}"
								BorderThickness="1"
								CornerRadius="3"
								Opacity="0" />

							<Grid
								x:Name="Content"
								Grid.Column="1"
								Background="Transparent">

								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition />
								</Grid.ColumnDefinitions>

								<ContentPresenter
									Width="48"
									Height="48"
									Margin="3"
									VerticalAlignment="Center"
									Content="{Binding .}"
									TextBlock.Foreground="{DynamicResource ForegroundBrush}">
									<ContentPresenter.Resources>
										<ResourceDictionary Source="./LibraryIconTemplates.xaml" />
									</ContentPresenter.Resources>
								</ContentPresenter>

								<ContentPresenter
									Grid.Column="1"
									VerticalAlignment="Center"
									Content="{Binding .}"
									TextBlock.Foreground="{DynamicResource ForegroundBrush}">
									<ContentPresenter.Resources>
										<ResourceDictionary Source="./LibraryInfoTemplates.xaml" />
									</ContentPresenter.Resources>
								</ContentPresenter>


							</Grid>

							<ToggleButton
								Height="Auto"
								Margin="8,1,2,1"
								VerticalAlignment="Stretch"
								Background="Transparent">
								<Grid>
									<wpfUtils:IconBlock
										Margin="6"
										Foreground="{DynamicResource ForegroundLightBrush}"
										Icon="Heart"
										IconStyle="OutlineThin" />
								</Grid>

							</ToggleButton>
						</Grid>
					</Border>

					<ControlTemplate.Triggers>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="Opacity" Value="0.5" />
						</Trigger>

						<DataTrigger
							Binding="{Binding IsMouseOver, ElementName=Content}"
							Value="True">
							<DataTrigger.EnterActions>
								<BeginStoryboard>
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetName="HoverBorder"
											Storyboard.TargetProperty="Opacity"
											To="1"
											Duration="0:0:0.100" />
									</Storyboard>
								</BeginStoryboard>
							</DataTrigger.EnterActions>

							<DataTrigger.ExitActions>
								<BeginStoryboard>
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetName="HoverBorder"
											Storyboard.TargetProperty="Opacity"
											From="1"
											To="0"
											Duration="0:0:0.250" />
									</Storyboard>
								</BeginStoryboard>
							</DataTrigger.ExitActions>
						</DataTrigger>

						<Trigger Property="IsSelected" Value="True">
							<Trigger.EnterActions>
								<BeginStoryboard>
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetName="SelectionBorder"
											Storyboard.TargetProperty="Opacity"
											To="1"
											Duration="0:0:0.100" />
									</Storyboard>
								</BeginStoryboard>
							</Trigger.EnterActions>

							<Trigger.ExitActions>
								<BeginStoryboard>
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetName="SelectionBorder"
											Storyboard.TargetProperty="Opacity"
											To="0"
											Duration="0:0:0.250" />
									</Storyboard>
								</BeginStoryboard>
							</Trigger.ExitActions>
						</Trigger>


					</ControlTemplate.Triggers>

				</ControlTemplate>

			</ResourceDictionary>
		</Grid.Resources>

		<ContentPresenter
			x:Name="SearchBar"
			HorizontalAlignment="Left"
			VerticalAlignment="Top"
			Content="{Binding .}">

			<ContentPresenter.ContentTemplate>
				<DataTemplate DataType="{x:Type local:QuickSearch}">
					<Grid>
						<Border
							x:Name="InputBorder"
							Width="300"
							Margin="26,18,0,0"
							VerticalAlignment="Center"
							Background="{DynamicResource BackgroundBrush}"
							Style="{DynamicResource WpfUtilsControlBorder}">

							<Grid>
								<Grid.RowDefinitions>
									<RowDefinition Height="Auto" />
									<RowDefinition />
								</Grid.RowDefinitions>

								<tags:TagSelector
									x:Name="SearchBox"
									Grid.Row="0"
									Height="32"
									Margin="34,2,2,2"
									AllTags="{Binding AvailableTags}"
									OnDone="OnSearchDone"
									Search="{Binding Search, Mode=TwoWay}"
									Tags="{Binding TagFilter.Tags}" />

								<Grid
									x:Name="ResultsArea"
									Grid.Row="1"
									Height="0">
									<ListBox
										Margin="3"
										ItemsSource="{Binding Results}"
										KeyDown="OnResultsListKeyDown"
										MouseDoubleClick="ResultsListDoubleClicked"
										ScrollViewer.VerticalScrollBarVisibility="Visible"
										SelectedItem="{Binding SelectedItem}"
										VirtualizingPanel.VirtualizationMode="Recycling">
										<ListBox.ItemContainerStyle>
											<Style
												BasedOn="{StaticResource WpfUtilsListBoxItem}"
												TargetType="{x:Type ListBoxItem}">
												<Setter Property="Template" Value="{StaticResource ListBoxItemTemplate}" />
											</Style>
										</ListBox.ItemContainerStyle>
									</ListBox>
								</Grid>
							</Grid>
						</Border>

						<Button
							Name="StudioButton"
							HorizontalAlignment="Left"
							VerticalAlignment="Top"
							IsHitTestVisible="False"
							Style="{StaticResource StudioButton}" />
					</Grid>

					<DataTemplate.Triggers>
						<DataTrigger
							Binding="{Binding IsQuickSearchOpen}"
							Value="False">
							<Setter Property="Opacity" Value="0" />
							<Setter Property="IsEnabled" Value="False" />
							<Setter Property="IsHitTestVisible" Value="False" />
							<Setter TargetName="SearchBox" Property="IsEnabled" Value="False" />
						</DataTrigger>
						<DataTrigger
							Binding="{Binding IsQuickSearchOpen}"
							Value="True">

							<Setter Property="IsEnabled" Value="True" />
							<Setter Property="IsHitTestVisible" Value="True" />
							<Setter TargetName="SearchBox" Property="IsEnabled" Value="True" />

							<DataTrigger.EnterActions>
								<BeginStoryboard>
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetProperty="Opacity"
											From="0"
											To="1"
											Duration="0:00:00.250">
											<DoubleAnimation.EasingFunction>
												<SineEase EasingMode="EaseOut" />
											</DoubleAnimation.EasingFunction>
										</DoubleAnimation>

										<DoubleAnimation
											Storyboard.TargetName="InputBorder"
											Storyboard.TargetProperty="Width"
											From="0"
											To="300"
											Duration="0:00:00.200">
											<DoubleAnimation.EasingFunction>
												<SineEase EasingMode="EaseOut" />
											</DoubleAnimation.EasingFunction>
										</DoubleAnimation>
									</Storyboard>
								</BeginStoryboard>
							</DataTrigger.EnterActions>
							<DataTrigger.ExitActions>
								<BeginStoryboard>
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetProperty="Opacity"
											From="1"
											To="0"
											Duration="0:00:00.250">
											<DoubleAnimation.EasingFunction>
												<SineEase EasingMode="EaseIn" />
											</DoubleAnimation.EasingFunction>
										</DoubleAnimation>
									</Storyboard>
								</BeginStoryboard>
							</DataTrigger.ExitActions>
						</DataTrigger>

						<DataTrigger
							Binding="{Binding Results.Count}"
							Value="0">
							<Setter TargetName="ResultsArea" Property="Height" Value="0" />
						</DataTrigger>

						<DataTrigger
							Binding="{Binding Results.Count, Converter={StaticResource NotZeroToBool}}"
							Value="True">
							<Setter TargetName="ResultsArea" Property="Height" Value="300" />
						</DataTrigger>

					</DataTemplate.Triggers>

				</DataTemplate>
			</ContentPresenter.ContentTemplate>
		</ContentPresenter>
	</Grid>
</windows:DockPanel>
