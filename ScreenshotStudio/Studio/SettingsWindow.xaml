<windows:PanelWindow
	x:Class="ScreenshotStudio.Studio.SettingsWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:ScreenshotStudio.Studio"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:windows="clr-namespace:ScreenshotStudio.Windows"
	Title="{DynamicResource LOC_Settings}"
	Width="300"
	Height="450"
	MinWidth="300"
	d:Background="{DynamicResource BackgroundBrush}"
	d:DataContext="{Binding Path=., RelativeSource={RelativeSource Mode=Self}}"
	TitleIcon="Gears"
	mc:Ignorable="d">
	<TabControl>

		<TabItem Header="Interface" />

		<TabItem Header="Files">
			<StackPanel>
				<GroupBox Header="Default Metadata">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition />
						</Grid.ColumnDefinitions>

						<Grid.RowDefinitions>
							<RowDefinition />
							<RowDefinition />
						</Grid.RowDefinitions>

						<TextBlock
							Grid.Row="0"
							Grid.Column="0"
							Style="{DynamicResource RightLabel}"
							Text="Author:" />
						<TextBox
							Grid.Row="0"
							Grid.Column="1"
							Text="{Binding Config.DefaultAuthor}" />

						<TextBlock
							Grid.Row="1"
							Grid.Column="0"
							Style="{DynamicResource RightLabel}"
							Text="Version:" />
						<TextBox
							Grid.Row="1"
							Grid.Column="1"
							Text="{Binding Config.DefaultVersion}" />
					</Grid>
				</GroupBox>

			</StackPanel>
		</TabItem>

		<TabItem Header="Input" />

	</TabControl>
</windows:PanelWindow>
